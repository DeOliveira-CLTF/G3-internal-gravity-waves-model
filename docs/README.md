# Documentation and references

The files in `archive/` are earlier course and manuscript materials retained
for provenance. The files in `references/` are background or assignment
documents. None is required to install or execute the repository.

The submitted article is maintained outside this repository; the root README
maps its experiment groups to the reproducible code and outputs.
